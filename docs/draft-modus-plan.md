# Draft-Modus Design Plan

**Audience:** an implementing agent working in this repo.
**Status:** Phases 1–3 shipped on `main`; Phases 4–6 below are the roadmap.

Referenced by `docs/draft-contract.md` and `docs/style-learning-contract.md`
as "the Draft-Modus design plan" — this file is that plan. It covers
scheduling, newsletter filtering, and macOS integration; the prompt-assembly
details those two contract docs govern are out of scope here.

Draft-Modus is opt-in (`SettingsStore.draftModeEnabled`, default off), never
touches Apple Mail's real Drafts folder, and stores everything in a separate
local database (`drafts.db`, decoupled from `askmail.db`) so deleting or
rebuilding one never touches the other.

---

## What's shipped

### Phase 1 — drafting core (`7d97d8f`)

Thread reconstruction (`ThreadResolver`, via `In-Reply-To`/`References`),
newsletter/bulk-mail classification (`NewsletterClassifier`), a shared
hybrid-retrieval helper extracted from `QueryService` (`Retriever`), prompt
assembly (`DraftAssembler`, `docs/draft-contract.md`), and the separate
`drafts.db` (`DraftStore`). Fully unit-testable, zero new OS permissions, no
background scheduling — this phase never runs anything on its own.

### Phase 2 — background orchestration (`a6be7d3`)

`DraftEngine`/`DraftScheduler` (`Sources/AskMailApp`) + `DraftJobProcessor`
(the testable core, `Sources/AskMailCore`): a fast envelope-index poll for new
Inbox mail, an FSEvents watch on `INBOX.mbox` with a 2-minute floor `Timer` as
a safety net, wake-from-sleep and launch catch-up, and a job queue
(`draft_jobs`) carrying each candidate through
`pending -> classifying -> {newsletterSkipped | autoGenerated | eligible} ->
drafting -> {drafted | failed, backoff-gated retry}`.

### Phase 3 — style learning (`2b71b86`, `aadb615`)

`StyleLearner` (`docs/style-learning-contract.md`): compares a generated
draft against the real Sent reply that eventually followed it, and folds the
delta into `global`/`domain`/`address`-scoped `style_profiles` rows via a
local-only LLM merge call, self-gated to once/24h. `DraftJobProcessor`
now looks up the best-available learned guidance (address > domain > global)
before assembling each draft — the `styleGuidance` hook Phase 1 built but
always passed `nil`.

While reviewing Phase 3, a **pre-existing Phase 2 gap** was also found and
fixed (`aadb615`, not really "Phase 3" work): `NewsletterClassifier`'s own
doc comment claimed a caller-side `noreply@`/`no-reply@` hard rule already
existed; it didn't. `NewsletterClassifier.isNoReplySender` now closes that,
checked the same way as the existing `isAutoGenerated` gate.

### Process note (recommend continuing for Phases 4–6)

Phases 2 and 3 were both built the same way: a research pass grounding the
design in the real codebase, direct implementation with continuous
build/test iteration, then a multi-dimension adversarial review (e.g.
concurrency, hardening/privacy, state-machine edge cases, integration with
prior phases) with every finding independently re-verified before being
trusted, then fixes. This caught real, non-obvious bugs both times (a
`MailboxWatcher` data race confirmed via ThreadSanitizer in Phase 2; a
system-vs-user prompt-channel gap and a sibling-draft double-counting bug in
Phase 3) that a single implementation-then-test pass did not surface.
Recommended for Phases 4–6 too, scaled to what each phase actually touches.

---

## Phase 4 — `DraftService`: macOS Services menu

**Goal:** let the user act on a stored draft from inside Mail.app's own
compose window, via the standard macOS Services mechanism — no new
permission, no new window of AskMail's own.

- "Insert" (paste the latest ready draft for the thread into the frontmost
  compose window) and "Regenerate" (re-run drafting on demand, bypassing the
  scheduler's cadence) as `NSServices` menu items, registered the normal way
  (`NSApplication.servicesProvider`, an `Info.plist` `NSServices` entry) —
  this is a standard, unprivileged macOS mechanism; no entitlement change.
- Reads from `drafts.db` (`DraftStore.latestDraft(threadID:)`, already
  shipped) keyed off whatever thread/message Mail.app hands the service.
  "Regenerate" calls into `DraftJobProcessor`'s existing drafting path
  directly (same `DraftAssembler`/`StyleLearner.guidance` wiring
  `draftOne` already uses) rather than duplicating it.
- Needs a live-Mac spike to confirm exactly what identifying information a
  Mail.app Service invocation actually hands the provider (a `NSAttributedString`/
  selection payload from the front document — not, notably, an
  AppleScript-level message id), and how to map that back to a `messageID`/
  `threadID` this app already has indexed. Do this spike before writing the
  provider; it decides how much of "Insert" is even feasible without Phase 5's
  Automation grant.
- **DoD:** with a drafted, `ready` thread open in Mail.app, the Services menu
  (Mail ▸ Services, or the contextual menu) shows "Insert draft" and
  "Regenerate draft"; invoking "Insert" pastes the exact `draft_text` at the
  compose cursor; invoking "Regenerate" replaces the stored draft with a
  freshly generated one and the next "Insert" reflects it. No FDA/Automation/
  Accessibility prompt appears at any point in this phase.

---

## Phase 5 — Global hotkey + Mail.app Automation

**Goal:** a hotkey-driven path to insert/regenerate without going through the
Services menu — AskMail's first-ever Automation grant.

**Blocked on interactive spikes on a live Mac before any design is finalized
here** (this is why this phase stays a stub, not a detailed plan, until
those run):
1. `sdef /Applications/Mail.app` — verify the actual AppleScript object model
   (message/mailbox/document hierarchy) on the target macOS version; prior
   phases' own experience (`Defaults.mailRoot`'s `V<n>` version-probing, the
   Accounts4.sqlite discovery in spike B11 #1) shows Apple's own on-disk/
   scripting surfaces drift across releases and must be verified live, never
   assumed from documentation.
2. Whether inserting text into Mail's compose window via Automation requires
   an Accessibility grant (synthetic keystroke/paste) or can go through a
   pure AppleScript/Apple Events object-model write. `docs/hardening.md`'s
   existing regression guard is explicit: **no `CGEventTap`, no Accessibility
   grant** ("Global hotkey via Carbon `RegisterEventHotKey`" — see
   `HotkeyManager.swift:40` — "no keystroke-interception surface"). If
   Automation-based insertion turns out to require Accessibility trust, this
   phase ships **clipboard-only** (copy the draft, hotkey surfaces a
   copy-and-notify affordance, user pastes manually) — never a transitional
   escalation to Accessibility as a stopgap.
- Once the spikes resolve those two questions, this section should be
  rewritten with the same level of detail as Phase 1's original plan (exact
  new types, exact `MailQuery.swift` surface, exact hardening-doc additions)
  before implementation starts.
- **DoD (provisional, pending the spikes):** the hotkey triggers either a
  live insertion into Mail's compose window or a clipboard-copy-and-notify
  fallback, whichever the spikes determine is safe; `docs/hardening.md`
  gains a new item documenting the Automation grant's scope and the
  Accessibility decision, with the same DoD rigor as existing H- items.

---

## Phase 6 — Settings UI

**Goal:** make Draft-Modus's existing (Phase 1–3) and forthcoming (Phase
4–5) behavior visible and configurable, matching `SettingsView.swift`'s
existing patterns rather than inventing new ones.

- **Master toggle + battery policy**: `draftModeEnabled`/`draftAllowOnBattery`
  already exist in `SettingsStore`; this phase is purely the UI for switches
  that already work end-to-end.
- **Sender exclusion list**: a user-configurable list of senders/domains to
  never draft for, checked by the caller before `NewsletterClassifier`
  the same way `isAutoGenerated`/`isNoReplySender` already are. Confirmed
  genuine new territory — no existing list-editor pattern in this codebase
  to reuse (verified twice now: once when Phase 1 was originally scoped, and
  again when Phase 3's review found `NewsletterClassifier`'s doc comment had
  been claiming this already existed since Phase 1/2, when it never did).
- **Status surfacing**: `DraftEngine.pendingCount`/`failedCount` already
  exist for the job queue (DM-12); this phase is the UI. Additionally —
  **new relative to the original Phase 6 scope**, since `StyleLearner` did
  not exist when Phase 6 was first sketched — surface per-scope
  `style_profiles.sample_count`/`updated_at` (e.g. "learned from N replies"
  globally and per top correspondent), mirroring the same transparency
  principle Q&A citations already give the user, extended to "what shaped
  this draft's tone."
- **Reset/clear learned style**: **new relative to the original Phase 6
  scope**, for the same reason — a `DraftStore` method to delete some or all
  `style_profiles` rows (mirroring `SQLiteStore.deleteAll()`/FR-8's "delete &
  rebuild" pattern) plus a Settings action to trigger it, so a user who
  doesn't like the learned tone isn't stuck with it, without needing a whole
  "delete & rebuild" of the entire drafts database.
- **Permission-prompt UI**: once Phase 5 lands, its Automation (and possibly
  Accessibility) grant needs an onboarding/status affordance. Follow
  `SettingsView.swift`'s actual `openFullDiskAccessSettings`-style pattern
  (a real Settings-pane deep link + status check), **not**
  `OnboardingChecklist.swift`, which is a plain data struct with no UI of its
  own.
- **`docs/hardening.md` DM section**: a dedicated table entry (or small
  section) for Draft-Modus's own hardening posture — the `drafts.db`
  `FileHardening.lockDown` coverage (already true since Phase 1), the
  local-only LLM guarantee (already true since Phase 2/3), and whatever
  Phase 5's Automation/Accessibility decision turns out to be — written with
  the same one-unambiguous-DoD-per-item rigor the rest of that file uses.
- **Full verification playbook**: a manual end-to-end pass exercising every
  Draft-Modus surface together (toggle on, wait for a real draft, Insert via
  Services, Regenerate, hotkey path, sender-exclusion list, style-profile
  reset), the same spirit as `docs/hardening.md`'s existing verification
  playbook.
- **DoD:** every toggle/list/status view reflects real `SettingsStore`/
  `DraftStore` state with no restart needed (FR-9 parity); the sender
  exclusion list actually prevents a draft for an excluded sender/domain
  (regression test); the style-profile reset actually empties `style_profiles`
  and a subsequent draft has no `styleGuidance` until new samples are
  learned again (regression test).
